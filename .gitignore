# Source de StepMania y YARG (clones de repos oficiales, referencias locales)
stepmania/
stepmania-5_1-new/
YARG-master/
AutoStepper-master/

# Configuración local de Claude Code y agentes
.claude/
.agent/

# Husky (hook roto: llama pnpm en proyecto sin package.json)
.husky/

# Node / Vitest
node_modules/
coverage/
.vitest/

# Variables de entorno y credenciales (NUNCA commitear)
.env
.env.*
!.env.example

# Sistema
Thumbs.db
desktop.ini
.DS_Store
PLAN-versus-2-jugadores.md
scripts/deploy.sh

# Fuentes PNG de la landing — solo se trackean y despliegan los .webp.
# Los .png originales viven localmente como fuente de re-export pero no
# entran en git para evitar duplicar peso (los WebP equivalentes ahorran
# ~92% del tamaño y son los que ve el usuario en producción).
sincro-stepmania-neon.png
sincro-guitar-neon.png
cerebro-sincro.png
corazon-sincro.png
